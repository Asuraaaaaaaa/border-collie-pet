# LinePuppy 边牧桌宠功能与技术实现

> LinePuppy 是一款运行在 macOS 和 Windows 桌面上的边牧桌面宠物，集成序列帧动画、番茄钟、键盘统计、备忘录、喝水提醒和好友聊天等功能。

- **当前版本**：`v0.3.6`
- **最后更新**：2026-07-30
- **桌面框架**：Tauri 2 + Rust
- **前端**：HTML + CSS + JavaScript ES Modules + Vite 8
- **云端服务**：Supabase Auth + PostgreSQL + Realtime
- **支持平台**：macOS、Windows

---

## 1. 产品形态

LinePuppy 以一个透明、无边框、默认始终置顶的小窗口运行。平时窗口与宠物图像保持同样大小；打开菜单、番茄钟键盘、备忘录提醒或聊天面板时，窗口会动态扩展，把面板作为指向宠物的对话气泡显示。

### 技术方案

- Tauri 窗口配置使用 `transparent: true`、`decorations: false`、`alwaysOnTop: true`、`skipTaskbar: true`。
- 窗口初始大小为 `164 x 164` 逻辑像素，内部保留透明安全边距；宠物默认显示为 `160 x 160`，可调范围为 `100-320` 像素。
- 前端通过 Tauri Window API 设置窗口尺寸和屏幕位置。
- 窗口几何更新使用“只执行最新任务”队列，合并快速连续发生的布局请求，减少 Windows 上的抖动和过期位置覆盖。

---

## 2. 宠物动画与行为状态机

### 2.1 已实现行为

| 行为 | 帧数 | 默认播放速度 | 使用场景 |
|---|---:|---:|---|
| 待机 `idle` | 4 | 1.5 FPS | 默认站立、眨眼 |
| 散步 `walk` | 4 | 8 FPS | 随机行为或手动互动 |
| 跑步 `run` | 4 | 10 FPS | 随机行为或手动互动 |
| 坐下 `sit` | 2 | 1.5 FPS | 随机行为、手动互动、番茄钟姿势 |
| 趴下 `lying` | 1 | 1 FPS | 长时间待机、手动睡觉、番茄钟姿势 |
| 睡觉 `sleep` | 1 | 1 FPS | 夜间自动睡觉或趴下后入睡 |
| 跳跃 `jump` | 3 | 6 FPS | 随机行为或手动互动 |
| 摇尾巴 `wag` | 4 | 4 FPS | 手动互动、备忘录到期提醒 |
| 歪头 `tilt` | 2 | 5 FPS | 随机行为或手动互动 |
| 嗅闻 `sniff` | 2 | 2 FPS | 随机行为或手动互动 |
| 抓挠 `scratch` | 2 | 4 FPS | 随机行为或手动互动 |
| 开心/摸头 `happy` | 2 | 5 FPS | 点击、休息、喝水或低电量反馈 |
| 拖拽 `drag` | 1 | 1 FPS | 按住宠物移动时 |

### 2.2 自主行为

- 行为循环约每 `33 ms` 更新一次。
- 散步速度为每次更新 `3` 逻辑像素，跑步为 `7` 逻辑像素。
- 宠物碰到当前显示器工作区边缘时自动转向，向左移动使用 CSS `scaleX(-1)` 翻转右向素材。
- 不同时段使用不同的加权随机行为表，例如早晨更活跃、中午更容易趴下、晚上更安静。
- 持续待机约 2400 个行为更新周期后会趴下，然后进入睡觉。

### 技术方案

- 宠物素材为透明 PNG 序列帧，由 Vite `import.meta.glob` 在构建时导入和生成带哈希的资源 URL。
- JavaScript 状态机管理当前行为、帧索引、持续时间、移动方向和锁定姿势。
- 初始化时预加载所有帧图片，避免第一次切换行为时出现空白或闪烁。

---

## 3. 鼠标与菜单互动

| 交互 | 效果 | 技术实现 |
|---|---|---|
| 左键单击 | 宠物做开心/歪头动画 | 通过鼠标按下和移动距离区分单击与拖拽 |
| 按住拖拽 | 宠物跟随鼠标移动 | 使用屏幕坐标和 Tauri `setPosition` 更新原生窗口位置 |
| 鼠标滚轮 | 以 16 px 步长缩放宠物 | 尺寸限制为 `100-320 px`，写入 `localStorage.petSize` |
| 右键 | 打开功能菜单 | 禁用浏览器默认菜单，改用 HTML/CSS 自定义面板 |
| 回到原位 | 回到当前屏幕右下区域 | 基于当前显示器工作区和宠物尺寸计算 |
| 重置大小 | 恢复为 `160 px` | 更新界面和本地持久化值 |

### 右键菜单功能

- 番茄钟
- 备忘录
- 喝水提醒
- 好友与消息
- 互动子菜单：去散步、跑起来、蹦一下、摸摸头、歪头杀、摇尾巴、嗅一嗅、挠痒痒、坐下、去睡觉
- 回到原位、重置大小、退出

菜单根据当前工作区自动选择在宠物上方或下方展开，并动态测量文字与控件占用空间。可用高度不足时菜单内部滚动，宠物位置不会因菜单被屏幕边缘截断而跳动。

---

## 4. 时间感知与桌面反馈

### 4.1 五个时段

| 时段 | 时间 | 行为特征 |
|---|---|---|
| 早晨 | 07:00-10:59 | 散步、跑步权重较高 |
| 中午 | 11:00-13:59 | 跑步权重降低，更容易坐下或趴下 |
| 下午 | 14:00-17:59 | 恢复正常的散步和跑步 |
| 晚上 | 18:00-21:59 | 行为更温和，待机权重更高 |
| 夜间 | 22:00-06:59 | 进入睡觉，白天后自动醒来 |

启动时根据时段显示“你好呀”、“中午好呀”或“夜深了，我先睡了”。

### 4.2 自适应说话气泡

- 短文本按内容宽度紧凑显示，最大宽度为 `200 px`。
- 长文本自动换行，使用 `overflow-wrap: anywhere` 防止长单词或长数字溢出。
- 可显示多行文本，并在指定时间后自动消失。
- 气泡用于启动问候、番茄钟、喝水、电量、备忘录和聊天通知等桌面反馈。

---

## 5. 番茄钟

### 用户功能

- 快速预设：`15 / 25 / 30 / 45 / 60` 分钟。
- 工作时长：`5-90` 分钟，每次调整 `5` 分钟。
- 休息时长：`1-30` 分钟。
- 循环次数：`1-10` 轮，默认 `4` 轮。
- 专注姿势：趴下、坐着或站立。
- 工作期显示实时倒计时、当前轮次和模拟键盘。
- 工作期结束时显示本轮总按键数和最常用的 3 个按键，然后进入休息。
- 休息期结束后自动进入下一轮；全部完成后显示完成提示。
- 可在右键菜单中随时停止。
- 工作期间拖动宠物时临时解锁姿势，松开后恢复用户选定的专注姿势。

### 技术方案

- 番茄钟是前端状态机，状态包含 `work/break`、剩余秒数、当前轮次和上次时间戳。
- 倒计时根据 `Date.now()` 的实际秒数差值递减，不是假设每个动画 Tick 都准时执行，可减少系统卡顿造成的计时漂移。
- 工作时长、休息时长、轮次和姿势写入 `localStorage`，下次启动仍保留。
- 专注时通过 `lockedState` 锁定宠物姿势。
- 专注时收到聊天消息不立即打断，只累加延后通知计数，休息时再统一提示。

---

## 6. 模拟键盘与打字统计

### 用户功能

- 仅在番茄钟工作期显示紧凑的对话气泡键盘。
- 物理按键被按下时，对应键位短暂高亮。
- 显示本轮累计按键数。
- 显示滚动过去 60 秒内的“键/分”数值。
- 统计本轮使用频率最高的 5 个物理按键，气泡总结显示前 3 个。
- 显示当前是“全局监听”还是“窗口监听”，全局监听失败后可点击重试。

### 隐私与统计边界

- 只记录标准物理按键代码，例如 `KeyA`、`Space`、`Enter`；不获取输入框文本，不拼接用户输入内容。
- 统计只存在当前运行进程内存中，不写入本地文件，不发送到 Supabase 或其他服务器。
- 空格、回车、修饰键等与字母键使用同一计数规则，不做语义排除。
- 全局监听激活时忽略窗口内的重复 `keydown`，防止同一次按键被计数两次。

### 平台实现

- **macOS**：Rust 使用 Core Graphics `CGEventTap`，并且以 `ListenOnly` 方式监听。启动前通过 `CGPreflightListenEventAccess` / `CGRequestListenEventAccess` 检查和请求“输入监控”权限。
- **Windows 及其他非 macOS 平台**：Rust 使用 `rdev` 监听全局 `KeyPress` 事件。
- Rust 端将按键映射为浏览器 `KeyboardEvent.code` 格式，再通过 Tauri Event 发送 `global-keydown` 给前端。
- 全局监听不可用时，前端回退到当前宠物窗口的 `keydown` 监听。
- 打开右键菜单、拖动宠物、聊天面板或备忘录到期提醒时，键盘面板会暂停并收起，已统计的本轮数据保留。

---

## 7. 备忘录与到期提醒

### 用户功能

- 在右键菜单中新建备忘录，内容最长 `200` 个字符。
- 每条备忘录必须设置晚于当前时间的到期时间。
- 新建时默认到期时间约为 30 分钟后，并向上取整到 5 分钟刻度。
- 未完成备忘录按到期时间升序展示，菜单项显示未完成数量。
- 支持编辑、完成和删除。完成后从未完成列表中移除。
- 到期后弹出红色脉冲提醒面板，宠物同时摇尾巴并显示红色发光效果。
- 多条备忘录同时到期时，先展示最早到期的一条，并提示队列中还有多少条待处理。
- 到期面板可直接标记完成，或延后 `5 / 15 / 30 / 60` 分钟再提醒。
- 根据屏幕剩余空间自动放在宠物上、下、左或右侧。

### 技术方案

- 备忘录由纯 JavaScript 领域逻辑管理，每条记录包含 `id`、`content`、`dueAt`、`createdAt`、`updatedAt`和 `completedAt`。
- ID 优先使用 `crypto.randomUUID()` 生成。
- 未完成记录以 JSON 写入 `localStorage.linePuppyMemos`，完成或删除后不再持久化。
- 启动时会容错解析本地数据，损坏或字段不完整的记录会被过滤，不会阻止应用启动。
- 每秒检查一次到期状态；窗口重新获得焦点或从后台恢复时也会立即检查。
- 到期提醒优先级高于聊天面板、菜单和番茄钟键盘；这些面板会暂时收起，完成或延后后恢复。
- 按当前要求，备忘录到期**没有提示音**。

---

## 8. 喝水提醒

- 可在右键菜单中开启或关闭，勾选状态实时反映开关。
- 开启后每 `45` 分钟检查一次并显示“该喝水了”，宠物做开心动画。
- 番茄钟工作期、宠物睡觉或正在拖动时，本次提醒被跳过，不打断当前操作。
- 使用前端 `setInterval` 实现，开关状态目前不持久化，每次启动默认关闭。

---

## 9. 低电量看护

- 当系统电量低于 `20%` 且未充电时，显示“电量低啦，快去充电”并做开心动画。
- 同一次低电量状态只提醒一次，电量恢复或插入充电器后重置状态。
- 使用 WebView 的 Battery Status API（`navigator.getBattery`）监听 `levelchange` 和 `chargingchange`。
- 该功能会检查 API 是否存在；当前 WebView/系统不提供该 API 时不启用，不影响其他功能。

---

## 10. 好友与消息

好友与消息是 `v0.3.3` 新增的在线功能。两位用户安装同一客户端后，可使用不同邮箱登录、交换好友码并聊天。

### 10.1 账号与登录

- 使用邮箱获取 `6` 位数字验证码，无需设置密码。
- 未注册邮箱第一次验证成功时自动创建账号。
- 首次登录需设置 `1-30` 个字符的昵称。
- 会话在本地持久化，Supabase JS 客户端自动刷新 Token；支持主动退出登录。

**技术方案**：使用 Supabase Auth `signInWithOtp` 和 `verifyOtp(type: "email")`，邮件由 Supabase 上配置的 SMTP 服务发送。桌面端的 Auth 和 REST 请求通过 Tauri HTTP 插件交给 Rust 网络栈发送，避免 Windows WebView 请求失败；网络权限仅开放当前 Supabase 项目。客户端只包含可公开的 Supabase Publishable Key，不包含数据库密码、SMTP 授权码或 Service Role Key。

### 10.2 个人资料与好友码

- 每个账号自动创建个人资料和唯一的 `8` 位好友码。
- 好友码由大写字母和数字组成，排除 `I / L / O / 0 / 1` 等容易混淆的字符。
- 支持一键复制自己的好友码。
- 用户可在设置中更改昵称和“桌面提醒显示消息正文”开关。

**技术方案**：`auth.users` 新用户触发 PostgreSQL Trigger，在 `profiles` 表创建资料。好友码由 `pgcrypto` 随机字节生成，数据库唯一索引防止重复。

### 10.3 好友申请

- 输入对方的 8 位好友码发送申请。
- “申请”页同时展示收到的申请和等待对方接受的外发申请。
- 接收方可接受或拒绝。
- 接受后自动建立好友关系、一对一会话和两位会话成员。
- 不存在的好友码、已是好友、已拉黑或重复申请都返回同类通用失败结果，减少通过好友码枚举用户的信息泄露。
- 每个账号 10 分钟内最多尝试 20 次好友码查找，24 小时前的记录会清理。

**技术方案**：申请和接受操作通过 PostgreSQL `SECURITY DEFINER` RPC 执行；双方 UUID 排序后用于唯一索引，并使用事务级 Advisory Lock 避免双方同时操作导致重复申请、重复好友关系或重复会话。

### 10.4 一对一聊天

- 消息长度为 `1-500` 个字符。
- 会话列表显示昵称、最后一条消息和未读数。
- 打开会话时自动标记为已读。
- 初次加载最近 50 条，可继续加载更早消息。
- 支持清空自己视图中的聊天记录、删除好友、拉黑和解除拉黑。
- “清空记录”只记录当前用户的清空水位，不物理删除对方的历史。
- 删除好友后保留历史会话，但禁止继续发送；拉黑还会取消双方待处理申请并阻止发送。

### 10.5 实时消息、断线恢复与通知

- 新消息通过 Supabase Realtime `postgres_changes` 订阅 `messages` 表的 `INSERT` 事件。
- Realtime 建立前先进入缓冲状态，完成初始同步后再合并期间收到的事件，减少订阅与历史查询之间的竞态丢消息。
- 每个会话按最后消息 ID 保存内存游标，重新联网、窗口获得焦点或从后台恢复时补拉遗漏消息。
- 消息按自增 `bigint id` 去重和排序，避免实时事件与补拉结果重复显示。
- 新消息未在当前会话内阅读时，右键菜单和会话列表显示未读数徽标。
- 默认桌面气泡只显示“某人发来了新消息”；用户开启正文预览后显示最多 60 个字符。
- 番茄钟工作期不立即显示消息气泡，休息时只汇总提示收到的消息数量。

---

## 11. 云端数据模型与安全边界

### 11.1 PostgreSQL 数据表

| 表 | 用途 |
|---|---|
| `profiles` | 用户昵称、好友码、消息预览设置 |
| `friend_requests` | 好友申请和处理状态 |
| `friendships` | 已建立的好友关系 |
| `blocks` | 单向拉黑关系 |
| `conversations` | 一对一会话 |
| `conversation_members` | 会话成员、已读水位和清空水位 |
| `messages` | 消息正文、发送人和时间 |
| `friend_lookup_attempts` | 好友码查找限流记录 |

### 11.2 数据库安全方案

- 所有社交表启用 PostgreSQL Row Level Security（RLS）。
- `anon` 角色不能读取社交表；`authenticated` 用户仅能读取自己、自己好友、自己申请或自己会话相关的数据。
- 客户端没有获得表的直接 `INSERT / UPDATE / DELETE` 权限；修改类操作必须经过受控数据库 RPC。
- RPC 使用 `auth.uid()` 识别用户，使用固定 `search_path = pg_catalog, public` 降低 `SECURITY DEFINER` 函数的搜索路径劫持风险。
- 消息表配置 `REPLICA IDENTITY FULL` 并加入 `supabase_realtime` Publication，Realtime 依然受用户 JWT 和 RLS 约束。
- Tauri CSP 将网络连接限制为应用自身与指定 Supabase HTTPS/WSS 地址。

### 11.3 数据加密边界

- 认证和传输经过 Supabase HTTPS/WSS，会话由 Supabase Auth JWT 保护。
- 聊天消息存储在 Supabase PostgreSQL 中，当前**不是端到端加密聊天**。
- 备忘录、宠物尺寸和番茄钟配置不上传云端。

---

## 12. 本地数据与云端数据

| 数据 | 保存位置 | 是否跨设备同步 |
|---|---|---|
| 宠物大小 | WebView `localStorage` | 否 |
| 番茄钟工作/休息/轮次/姿势 | WebView `localStorage` | 否 |
| 备忘录 | WebView `localStorage` | 否 |
| 喝水提醒开关 | 仅运行时内存 | 否 |
| 键盘统计 | 仅运行时内存 | 否，且不上传 |
| Supabase 登录会话 | Supabase JS 本地持久化 | 可在各设备分别登录 |
| 昵称、好友、申请、聊天、拉黑 | Supabase PostgreSQL | 是 |

---

## 13. 多显示器、DPI 和面板布局

- 通过 Tauri `currentMonitor()` 读取当前显示器，启动时无法获取时回退到 `primaryMonitor()`。
- 使用显示器 `scaleFactor` 把物理工作区转换为逻辑坐标，适配 Windows `100% / 125% / 150%` 缩放。
- 支持左侧或上方副屏带来的负坐标工作区。
- 拖拽结束、系统缩放变化、打开面板时重新检查工作区并限制宠物位置。
- 键盘、聊天和备忘到期面板按“上、下、右、左”的优先级寻找可用空间；都放不下时选择剩余空间最大的方向并夹紧在工作区内。
- 面板使用实际 `scrollWidth / scrollHeight`、边框尺寸和最小/最大尺寸动态测量，兼容 Windows 不同字体和表单控件度量。

---

## 14. 系统托盘与平台集成

### 托盘菜单

- 显示宠物
- 隐藏宠物
- 切换始终置顶
- 关于
- 退出

### 技术方案

- Rust 使用 Tauri `tray-icon` 构建原生系统托盘和菜单。
- macOS 启动后设置 `ActivationPolicy::Accessory`，隐藏 Dock 图标，但保留菜单栏入口。
- Windows 窗口使用 `windows_subsystem = "windows"`，Release 运行时不显示命令行黑窗口。
- Tauri 原生命令 `start_keyboard_listener` 是当前唯一个前端可调用的 Rust Command；其他互动通过 Window/Event API 完成。

---

## 15. 构建与发布

### 15.1 本地开发

```bash
npm install
npm run dev
```

- `npm run dev` 同时启动 Vite 开发服务器和 Tauri 应用。
- Vite 监听 `127.0.0.1:1420`，Tauri 开发 CSP 只额外允许该本地地址。

### 15.2 本地构建

```bash
npm run build
```

- `tauri build` 先执行 `npm run frontend:build`，Vite 将产物写入 `src/dist`。
- Tauri 从 `src/dist` 封装当前平台安装包。
- Rust Release Profile 启用 LTO、单 codegen unit、体积优化、符号剥离和 `panic = "abort"`。
- macOS 上不直接交叉生成 Windows 图形应用安装包，Windows 发布由 GitHub Actions 执行。

### 15.3 Windows 自动安装包

GitHub Actions 工作流位于 `.github/workflows/windows-release.yml`：

1. `workflow_dispatch` 手动运行：在 `windows-latest` 上构建 NSIS `.exe`，作为 Actions Artifact 上传，不创建 Release。
2. 推送 `v*` 标签：先校验版本、运行测试、构建 Windows x64 NSIS `.exe`，然后创建正式 GitHub Release 并附加安装包。
3. 标签版本必须与 `package.json`、`src-tauri/Cargo.toml` 和 `src-tauri/tauri.conf.json` 三处完全一致。
4. CI 使用 Node.js 22、Rust Stable、`npm ci`、Rust 依赖缓存和 `tauri-apps/tauri-action`。

当前 Windows 安装包未做商业代码签名，Microsoft Defender SmartScreen 可能显示“未知发布者”提示。

---

## 16. 测试与质量保障

### 测试命令

```bash
npm test
```

该命令依次运行：

- Node.js 内置 Test Runner：`tests/*.test.js`
- Rust 单元测试：`cargo test --manifest-path src-tauri/Cargo.toml`

### 当前覆盖重点

- 按键映射、全局/窗口监听状态和去重。
- 番茄钟统计、拖动后姿势恢复。
- 备忘录创建、解析容错、排序、到期、完成、延后、编辑和删除。
- Windows DPI、负坐标多屏、菜单/键盘/聊天面板布局。
- Supabase 配置不包含服务端密钥、社交输入规范化、RPC 路由、消息合并、通知隐私和实时订阅释放。
- Supabase Migration 的表、RPC、RLS、授权和 Realtime 声明。
- GitHub Actions 触发方式、测试顺序、版本校验和 Release/Artifact 行为。

`supabase/tests/social_chat_test.sql` 还提供基于 pgTAP 的数据库集成测试，覆盖好友申请、接受、发消息、未读水位、单方清空、删除好友、拉黑和跨会话隔离。

---

## 17. 当前能力边界与已知限制

- 好友与消息依赖互联网、Supabase 项目和邮件验证码服务。
- 当前仅支持一对一文本聊天，不支持群聊、图片、文件、语音或端到端加密。
- 聊天和备忘录通知是宠物窗口内气泡/面板，不是操作系统原生通知，也没有提示音。
- 备忘录是单设备本地功能，卸载、清理 WebView 数据或换电脑后不会从云端恢复。
- 喝水提醒开关不记忆，重启后需重新开启。
- 低电量功能取决于当前系统 WebView 是否开放 Battery Status API。
- macOS 全局键盘监听需要“输入监控”权限；未授权或监听失败时只能统计宠物窗口获得的按键。
- Windows 安装包当前未签名，首次下载/安装可能触发 SmartScreen。
- 系统托盘“关于”弹窗中的版本文案目前仍为硬编码 `v0.3.0`，与实际版本 `v0.3.6` 不一致，不影响其他功能。

---

## 18. 主要代码与职责

| 文件 | 职责 |
|---|---|
| `src/pet.js` | 宠物行为、拖拽、菜单、番茄钟、喝水、电量、备忘录和整体布局协调 |
| `src/logic.js` | 纯函数领域逻辑：屏幕坐标、面板布局、键盘状态、备忘录 |
| `src/keyboard.js` | 模拟键盘 UI、按键统计和全局/本地监听协调 |
| `src/social-logic.js` | 社交输入校验、消息去重排序、未读与通知逻辑 |
| `src/social-service.js` | Supabase Auth、PostgREST/RPC、Realtime 服务封装 |
| `src/social-ui.js` | 登录、好友、申请、会话、设置和实时同步 UI 控制器 |
| `src/index.html` | 页面结构和界面样式 |
| `src-tauri/src/main.rs` | Tauri 应用启动、托盘菜单和原生窗口集成 |
| `src-tauri/src/keyboard.rs` | macOS/Windows 全局键盘监听、权限检查和按键映射 |
| `supabase/migrations/202607300001_social_chat.sql` | 社交数据库表、索引、Trigger、RPC、RLS、授权和 Realtime |
| `.github/workflows/windows-release.yml` | Windows 测试构建、Artifact 和 GitHub Release 发布 |

---

## 19. 版本变更摘要

| 版本 | 日期 | 主要变更 |
|---|---|---|
| `v0.1.0` | 2026-07-23 | 透明窗口、序列帧动画、行为状态机、托盘菜单 |
| `v0.1.1` | 2026-07-24 | 替换边牧素材，增加跑步、睡觉和中文菜单 |
| `v0.1.2` | 2026-07-25 | 番茄钟、喝水提醒、时间感知、低电量看护 |
| `v0.2.0` | 2026-07-28 | 37 帧素材、更多行为、滚轮缩放和自适应菜单 |
| `v0.3.0` | 2026-07-29 | 全局键盘监听、键位映射、多显示器布局和测试 |
| `v0.3.1` | 2026-07-29 | 模拟键盘对话气泡与键/分统计等优化 |
| `v0.3.2` | 2026-07-29 | Windows 布局适配和自动发版优化 |
| `v0.3.3` | 2026-07-30 | 备忘录与到期提醒；邮箱 OTP 登录、好友申请、一对一实时聊天与未读通知 |
| `v0.3.4` | 2026-07-30 | 修复 Windows 启动时键盘面板错误显示和宠物无法操作 |
| `v0.3.5` | 2026-07-30 | 修复 Windows 右键菜单与备忘录面板裁切 |
| `v0.3.6` | 2026-07-30 | 修复 Windows 宠物脚部裁切、备忘录新增入口和邮箱验证码网络请求 |

---

_`FEATURES.md` 以当前代码为准。增加、删除或调整用户功能时，应同步更新本文档。_
