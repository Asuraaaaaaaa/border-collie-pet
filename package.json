{
  "name": "line-puppy-pet",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri",
    "test:js": "node --test tests/*.test.js",
    "test": "npm run test:js && cargo test --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "yaml": "^2.9.0"
  }
}
